There will be 3 applications in this project

1. Profiles
    -- Profile (Model) : will use django signals
    -- Relatiosnhip (model) : will help us manage         relations (Sender,Receiver,Status of relationship)

2. Posts
    -- Posts(model)
    -- Comment(model)
    -- Like(model)
    
3. allauth (authentication)
